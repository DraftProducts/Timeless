import 'package:flutter/material.dart';

class Activity {
  final int id;
  final String name;
  final Color color;
  final DateTime createdAt;

  Activity(this.id, this.name, this.color, this.createdAt);
}